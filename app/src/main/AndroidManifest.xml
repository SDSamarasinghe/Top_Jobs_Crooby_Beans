<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.topjobs">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TopJobs">
        <activity
            android:name=".My_requested_listings"
            android:exported="false"
            android:label="@string/title_activity_sample"
            android:theme="@style/Theme.TopJobs.NoActionBar" />

        <activity
            android:name=".View_requested_job_view"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustResize|stateHidden"
            android:theme="@style/AppCompatTheme" android:screenOrientation="portrait"/>
        <activity
            android:name=".Request_a_job_page"
            android:exported="false" />

        <activity
            android:name=".Create_Job"
            android:exported="false" />
        <activity
            android:name=".My_job_listings"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>